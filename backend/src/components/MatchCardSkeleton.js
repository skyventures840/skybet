import SkeletonLoader from './SkeletonLoader';

// Legacy wrapper for backward compatibility
const MatchCardSkeleton = () => {
  return <SkeletonLoader type="match-card" />;
};

export default MatchCardSkeleton;